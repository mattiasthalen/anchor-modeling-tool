repos:
  - repo: local
    hooks:
      - id: full-test
        name: Run full test suite (ruff, mypy, pytest)
        entry: make full-test
        language: system
        pass_filenames: false
        always_run: true
